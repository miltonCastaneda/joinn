import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { Referenciar } from './referenciar';

@NgModule({
  declarations: [
    Referenciar,
  ],
  imports: [
    IonicPageModule.forChild(Referenciar),
  ],
})
export class ReferenciarModule {}
